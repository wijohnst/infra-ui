import * as React from "react";

import { SemanticElement } from "./Footer.styles";

type FooterProps = {};

export const Footer = ({}: FooterProps): React.ReactElement => {
  return <SemanticElement></SemanticElement>;
};
