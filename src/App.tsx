import styled from "styled-components";
const AppWrapper = styled.div`
  display: flex;
  flex-flow: column;
  justify-content: center;
  align-items: center;
`;

function App() {
  return (
    <AppWrapper>
      <h1>App</h1>
    </AppWrapper>
  );
}

export default App;
