import styled from "styled-components";

export const HomeContainer = styled.div`
  background-color: var(--technical-light-gray);

  margin: var(--layout-size-md);
  padding: var(--layout-size-md);

  border-radius: var(--layout-size-sm);
`;
